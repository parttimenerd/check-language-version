package me.bechberger.sizes.programs.collections;

import java.util.HashMap;

/** Empty HashMap: object with no table yet (implementation-dependent). */
class HashMapSize0 {
    HashMap<Integer, Integer> value = new HashMap<>();
}