package me.bechberger.sizes.programs.collections;

import java.util.ArrayList;

/** ArrayList with 100 elements: deeper graph (many Integer boxes). */
class ArrayListSize100 {
    ArrayList<Integer> value = new ArrayList<>(100);
}