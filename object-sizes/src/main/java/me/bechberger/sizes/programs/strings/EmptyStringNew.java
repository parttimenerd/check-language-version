package me.bechberger.sizes.programs.strings;

/* Empty string created with new String("") (still may share backing, but it's a separate String object). */
class EmptyStringNew {
    String value = new String("");
}