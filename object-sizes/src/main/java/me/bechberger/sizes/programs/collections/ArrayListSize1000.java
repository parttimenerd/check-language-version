package me.bechberger.sizes.programs.collections;

import java.util.ArrayList;

/** ArrayList with 1000 elements: larger graph, still manageable. */
class ArrayListSize1000 {
    ArrayList<Integer> value = new ArrayList<>(1000);
}